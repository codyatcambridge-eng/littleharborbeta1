/**
 * Empty posts file.
 * Forum posts are now stored and fetched from Supabase via real API calls.
 * This file is kept for backward compatibility but contains no mock data.
 */

export const POSTS: any[] = [];

export const postById = (id: string) => null;
