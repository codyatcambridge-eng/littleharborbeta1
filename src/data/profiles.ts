/**
 * Empty profiles file.
 * Profiles are now created dynamically in Supabase when users sign up.
 * This file is kept for backward compatibility but contains no mock data.
 */

export const PROFILES: any[] = [];

export const profileById = (id: string) => null;

/** The "logged in" mock user is no longer used. */
export const CURRENT_USER = null;
